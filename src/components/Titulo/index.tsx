import styled from 'styled-components'

const Titulo = styled.h1`
    color: var(--cinza);
    font-size: 24px;
    font-weight: 700;
    line-height: 28px;
    margin: 1rem 0;
    text-align: center;
`

export default Titulo
